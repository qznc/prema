extends base

block body
	- foreach (err; errors)
		p(class="error") #{err}
	form(method="POST")
		p
			| I predict that
			input(type="text", value="", name="prediction")
			| and we can trade it until
			input(type="text", value="#{suggested_end}", name="end")
			| .
		p
			button Create
		p Settlement might cost up to #{max_loss}€.

